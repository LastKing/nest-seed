{
  semanticCommits: 'auto',
  packageRules: [
    {
      matchDepTypes: ['dependencies', 'devDependencies'],
      automerge: true,
    },
    {
      matchPackageNames: ['rimraf'],
      enabled: false,
    },
  ],
  extends: ['config:base'],
}
